# A local Postgres for `bun run dev` and the DB-gated tests.
DATABASE_URL=postgres://localhost:5432/postgres

# Optional example credential (see src/config). Delete when you add real config.
MODULE_SAMPLE_GREETING=hello
